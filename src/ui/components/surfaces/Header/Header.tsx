import React from 'react';
import HeaderApp from './Header.style';

const Header: React.FC = () => {
    return (
        <HeaderApp> 
            {/* usar header do material ui */}
        </HeaderApp>
    );
};

export default Header;